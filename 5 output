#include <stdio.h>
int main(void) {
    FILE *fp = fopen("test.txt", "w");
    fprintf(fp, "Hello, OS Lab!\n");
    fclose(fp);
    fp = fopen("test.txt", "r");
    char ch;
    while ((ch = fgetc(fp)) != EOF)
        putchar(ch);
    fclose(fp);
    return 0;
}
